swagger: '2.0'
info:
  version: 1.0.0
  title: Fixture API
host: httpbin.org

schemes:
  - http
consumes:
  - application/json
produces:
  - application/json
definitions:
  Foo:
    type: object
    required: [id, name]
    properties:
      id:
        type: integer
      name:
        type: string
paths:
  /get:
    get:
      summary: Test path
      operationId: getSomething
      parameters:
      - name: bar
        in: path
        type: string
        required: true
      - name: body
        in: body
        required: true
        schema:
          $ref: "#/definitions/Foo"
      responses:
        200:
          description: Foo created
          schema:
            $ref: "#/definitions/Foo"

